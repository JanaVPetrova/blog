= simple_form_for @subject, url: url, defaults: { input_html: { class: 'form-control' } } do |f|
  = f.input :title
  = f.button :submit, :class => 'btn-primary'
  = submit_tag t('cancel'), :type => :reset, :class => "btn btn-danger"